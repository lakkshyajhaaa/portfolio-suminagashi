
# Project Typography
Always use `whitespace-nowrap` for massive brutalist typography (like project titles on the Work page). Massive text should never break into multiple lines. If it overflows, it should overflow the bounding box to maintain the monolithic aesthetic. Also, keep project names in frontmatter concise.
